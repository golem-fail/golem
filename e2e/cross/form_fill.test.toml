[flow]
name = "Cross-platform complete form fill flow"
tags = ["cross-platform", "form", "text-input", "interaction"]

[[flow.apps]]
name = "app"
bundle = "com.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "fill_form_fields"
steps = [
  { action = "wait", text = "Enter email", timeout = 10000 },

  # Type into the email field and verify
  { action = "type", input = "Enter email", text = "testuser@golem.com" },
  { action = "assert_text", text = "testuser@golem.com" },

  # Type into the password field
  { action = "type", input = "Password", text = "P@ssw0rd!123" },

  # Type into the search field with a deliberate typo
  { action = "type", input = "Search", text = "golem testt" },
  { action = "assert_text", text = "golem testt" },

  # Use backspace to correct the typo
  { action = "backspace", text = "Search", count = 1 },
  { action = "assert_text", text = "golem test" },

  # Type the correct ending
  { action = "type", input = "Search", text = " query" },
  { action = "assert_text", text = "golem test query" },

  # Hide the keyboard
  { action = "hide_keyboard" },
]

[[block]]
name = "fill_multiline_and_submit"
steps = [
  # Type into the multiline text area
  { action = "type", input = "Multi-line text", text = "First line of input\nSecond line with details\nThird line conclusion" },

  # Hide the keyboard
  { action = "hide_keyboard" },

  # Read back form values to verify
  { action = "read", text = "Enter email", save_to = "filled_email" },
  { action = "read", text = "Search", save_to = "filled_search" },

  # Take a screenshot before submitting
  { action = "screenshot", path = "/tmp/golem-cross-form-filled.png" },

  # Verify the submit button is enabled
  { action = "assert_enabled", text = "Submit" },

  # Submit the form
  { action = "tap", text = "Submit" },

  # Take a screenshot after submission
  { action = "screenshot", path = "/tmp/golem-cross-form-submitted.png" },
]
