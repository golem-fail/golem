[flow]
name = "Screenshot test"
tags = ["cross-platform", "capture"]

[[flow.apps]]
name = "app"
bundle = "fail.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "screenshot_tests"
steps = [
  { action = "assert_visible", on_text = "Submit", timeout = 30000 },

  # Take a screenshot of the initial state
  { action = "screenshot", path = "/tmp/golem-screenshot-initial.png" },

  # Interact with the app to change state
  { action = "tap", on_text = "+" },
  { action = "tap", on_text = "+" },
  { action = "tap", on_text = "+" },
  { action = "type", on_text = "Enter email", input = "screenshot@test.com" },
  { action = "hide_keyboard" },

  # Take a screenshot after changes
  { action = "screenshot", path = "/tmp/golem-screenshot-after-interaction.png" },

  # Toggle dark mode and capture
  { action = "tap", on_text = "Dark Mode", auto_scroll = true },
  { action = "screenshot", path = "/tmp/golem-screenshot-dark-mode.png" },
]
