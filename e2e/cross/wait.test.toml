[flow]
name = "Wait and wait_not test"
tags = ["cross-platform", "synchronization"]

[[flow.apps]]
name = "app"
bundle = "com.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "wait_for_elements"
steps = [
  # Wait for the main UI to be ready
  { action = "wait", on_text = "Submit", timeout = 10000 },
  { action = "wait", on_text = "Counter", timeout = 5000 },
  { action = "wait", on_text = "Enter email", timeout = 5000 },

  # Wait for a specific text to be visible
  { action = "wait", on_text = "Item 0", timeout = 5000 },

  # Trigger an alert, then wait for it to appear, then dismiss it
  { action = "tap", on_text = "alert-button" },
  { action = "wait", on_text = "OK", timeout = 5000 },
  { action = "tap", on_text = "OK" },

  # wait_not: wait for the alert to disappear after dismissal
  { action = "wait_not", on_text = "Show Alert", timeout = 5000, on_fail = "skip" },

  # Wait for the counter to show initial value
  { action = "wait", on_text = "Counter", timeout = 3000 },
]
