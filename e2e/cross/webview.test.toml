[flow]
name = "Cross-platform WebView element interaction test"
tags = ["cross-platform", "webview", "interaction"]

[[flow.apps]]
name = "app"
bundle = "com.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "webview_element_interaction"
steps = [
  # Launch the app
  { action = "launch", app = "com.golem.test" },
  { action = "wait", accessibility_id = "submit-button", timeout = 10000 },

  # Verify core UI elements are visible (proxy for webview context loaded)
  { action = "assert_visible", accessibility_id = "email-input" },
  { action = "assert_visible", accessibility_id = "password-input" },
  { action = "assert_visible", accessibility_id = "search-input" },
  { action = "assert_visible", accessibility_id = "submit-button" },
  { action = "assert_visible", accessibility_id = "counter-display" },

  # Interact with text input elements inside the web view
  { action = "type", accessibility_id = "email-input", text = "webview@test.com" },
  { action = "assert_text", accessibility_id = "email-input", text = "webview@test.com" },

  { action = "type", accessibility_id = "search-input", text = "webview search query" },
  { action = "assert_text", accessibility_id = "search-input", text = "webview search query" },
  { action = "hide_keyboard" },

  # Interact with buttons inside the web view
  { action = "tap", accessibility_id = "increment-button" },
  { action = "tap", accessibility_id = "increment-button" },
  { action = "assert_text", accessibility_id = "counter-display", text = "2" },

  # Read values from web view elements
  { action = "read", accessibility_id = "counter-display", save_to = "counter_value" },
  { action = "read", accessibility_id = "orientation-label", save_to = "orientation" },
  { action = "read", accessibility_id = "theme-label", save_to = "theme" },

  # Verify device state labels are rendered in the web view
  { action = "assert_text", accessibility_id = "orientation-label", text = "Portrait" },
  { action = "assert_text", accessibility_id = "theme-label", text = "Light" },

  # Scroll within the web view to find list elements
  { action = "swipe", direction = "down" },
  { action = "swipe", direction = "down" },
  { action = "wait", accessibility_id = "list-item-0", timeout = 5000 },

  # Scroll back up to the top of the web view
  { action = "swipe", direction = "up" },
  { action = "swipe", direction = "up" },
  { action = "wait", accessibility_id = "submit-button", timeout = 5000 },

  # Take a screenshot to capture final web view state
  { action = "screenshot", path = "/tmp/golem-cross-webview.png" },
]
