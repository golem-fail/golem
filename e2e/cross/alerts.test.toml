[flow]
name = "Alerts test"
tags = ["cross-platform", "interaction", "alerts"]

[[flow.apps]]
name = "app"
bundle = "com.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "alert_tests"
steps = [
  { action = "launch", app = "com.golem.test" },
  { action = "wait", text = "Submit", timeout = 30000 },

  # Trigger a simple alert (button is off-screen, use auto_scroll)
  { action = "tap", text = "alert-button", auto_scroll = true },

  # Wait for the alert to appear and dismiss it
  { action = "wait", text = "OK", timeout = 5000 },
  { action = "tap", text = "OK" },

  # Trigger a confirmation dialog
  { action = "tap", text = "confirm-button" },

  # Wait for the confirm dialog and accept it
  { action = "wait", text = "Confirm", timeout = 5000 },
  { action = "tap", text = "Confirm" },

  # Trigger the confirmation dialog again and cancel it
  { action = "tap", text = "confirm-button" },
  { action = "wait", text = "Cancel", timeout = 5000 },
  { action = "tap", text = "Cancel" },

  # Trigger an action sheet
  { action = "tap", text = "action-sheet-button" },
  { action = "wait", text = "Action Sheet", timeout = 5000 },
]

# Dismiss action sheet: Android uses back button, iOS taps Cancel
[[block]]
name = "dismiss_action_sheet_android"
where = { os = "android" }
steps = [
  { action = "press", button = "back" },
]

[[block]]
name = "dismiss_action_sheet_ios"
where = { os = "ios" }
steps = [
  { action = "tap", text = "Cancel" },
]
