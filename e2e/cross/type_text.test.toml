[flow]
name = "Type and backspace test"
tags = ["cross-platform", "interaction", "text-input"]

[[flow.apps]]
name = "app"
bundle = "com.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "type_into_fields"
steps = [
  { action = "launch", app = "com.golem.test" },

  # Type into the email field
  { action = "type", placeholder = "Enter email", text = "user@example.com" },
  { action = "assert_text", placeholder = "Enter email", text = "user@example.com" },

  # Type into the password field
  { action = "type", placeholder = "Password", text = "s3cret!" },

  # Type into the search field
  { action = "type", placeholder = "Search", text = "hello world" },

  # Backspace to correct a typo in search
  { action = "backspace", placeholder = "Search", count = 5 },
  { action = "type", placeholder = "Search", text = "earth" },

  # Type into the multiline text area
  { action = "type", placeholder = "Multi-line text", text = "Line one\nLine two\nLine three" },

  # Hide the keyboard after typing
  { action = "hide_keyboard" },
]
