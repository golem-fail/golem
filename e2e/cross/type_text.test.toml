[flow]
name = "Type and backspace test"
tags = ["cross-platform", "interaction", "text-input"]

[[flow.apps]]
name = "app"
bundle = "fail.golem.test"

[[flow.apps.devices]]
os = "ios:latest"
type = "phone"

[[flow.apps.devices]]
os = "android:latest"
type = "phone"

[[block]]
name = "type_into_fields"
steps = [
  # Type into the email field
  { action = "type", on_text = "Enter email", input = "user@example.com" },
  { action = "assert_visible", on_text = "user@example.com" },

  # Type into the password field
  { action = "type", on_text = "Password", input = "s3cret!" },

  # Type into the search field
  { action = "type", on_text = "Search", input = "hello world" },

  # Backspace to correct a typo in search
  { action = "backspace", on_text = "hello world", count = 5 },
  { action = "type", on_text = "hello ", input = "earth" },

  # Type into the multiline text area
  { action = "type", on_text = "Multi-line text", input = "Line one\nLine two\nLine three" },

  # Hide the keyboard after typing
  { action = "hide_keyboard" },
]
