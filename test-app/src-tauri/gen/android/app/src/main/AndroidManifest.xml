<?xml version="1.0" encoding="utf-8"?>
<!--
    Tracked in git despite `gen/` being gitignored — see the carve-out
    in the repo's .gitignore. The `uses-permission` lines below are
    what `permissions_*.test` exercises via `pm grant`; without them
    Android refuses the runtime grant. Tauri 2.x has no first-class
    config for Android `<uses-permission>`, so we commit the
    auto-generated manifest directly.

    Neither `tauri android init` nor `tauri android build` overwrites a
    file that already exists, so tracking it is enough to keep it. The
    one exception is the deep-link intent-filter — see the comment on it
    below before adding anything near those markers.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Photos: Android 13+ uses READ_MEDIA_IMAGES, Android 14+ also
         honours READ_MEDIA_VISUAL_USER_SELECTED, and pre-13 still maps
         to READ_EXTERNAL_STORAGE. The shorthand normalizer in
         `golem-driver/src/android.rs` picks one branch per SDK level —
         all three are declared so `pm grant` accepts whichever the
         normalizer asks for on a given API level. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.golem_test_app"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Custom scheme for the deep_link e2e flows, kept OUTSIDE the
                 block above on purpose. `tauri-plugin-deep-link`'s build
                 script rewrites everything between those two markers from
                 `tauri.conf.json` every time the plugin crate is rebuilt
                 (fresh clone, `cargo clean`, dependency bump), so anything
                 added in there is deleted without warning. The config can't
                 carry this either: a `plugins.deep-link.mobile` entry is an
                 App Link associated domain (`host`/`pathPrefix`), and the
                 `schemes` key the plugin reads lives under `desktop` — a
                 custom scheme on Android has nowhere to go but here.

                 Self-contained by necessity: Android matches each
                 intent-filter as a whole, so the action and categories are
                 repeated rather than inherited. No `autoVerify` — that is
                 for App Links, and a non-http scheme inside a verified
                 filter risks Android rejecting the filter outright. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="golem-test" android:host="*" />
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
            <intent-filter android:autoVerify="true" >
                <action android:name="android.intent.action.VIEW" />
                <!-- ChromeOS ARC++ uses a different action for deep links -->
                <action android:name="org.chromium.arc.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="*" />
                
                
                
                
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
