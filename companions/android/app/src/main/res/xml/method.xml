<?xml version="1.0" encoding="UTF-8"?>
<!-- IME metadata for GolemImeService. A single generic subtype is
     declared so `ime enable`/`ime set` can target it on all API levels. -->
<input-method xmlns:android="http://schemas.android.com/apk/res/android">
    <subtype
        android:label="Golem"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true" />
</input-method>
